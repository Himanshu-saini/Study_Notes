========
MetaMask
========

- an ethereum wallet, running as a chrome extension
- It injects the ethereum web3 API into every website’s 
  javascript context, so that those apps can read from 
  the blockchain.
- also lets the user create and manage their own identities
  - the user gets a secure interface to review the transaction, 
    before approving or rejecting it.
